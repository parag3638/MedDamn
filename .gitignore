# ── Dependencies ─────────────────────────────────────────
node_modules/
.pnpm-store/
.bun/

# ── Next.js build output ────────────────────────────────
.next/
out/
.next-static/            # in case of custom setups
.vercel/                 # Vercel metadata

# ── Env files (keep secrets out of git) ─────────────────
.env
.env.local
.env*.local
.env.development
.env.test
.env.production

# ── Logs & debug ────────────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun.lockb
*.log
*.pid
*.seed
*.pid.lock

# ── Caches ──────────────────────────────────────────────
.cache/
.tmp/
temp/
tmp/
.cache-loader/
.eslintcache
.stylelintcache
.turbo/
.swp
.sass-cache/
.parcel-cache/
.swc/

# ── TypeScript / Build artifacts ────────────────────────
dist/
build/
*.tsbuildinfo

# ── Testing ─────────────────────────────────────────────
coverage/
.cypress/
cypress/screenshots/
cypress/videos/
playwright-report/
test-results/

# ── IDE / OS junk ───────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# ── Misc tools ──────────────────────────────────────────
*.local
*.orig
*.rej

# ── Lockfiles (optional policy) ─────────────────────────
# Uncomment exactly one if you enforce a single lockfile
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# ── Images/build outputs generated at runtime ───────────
public/_next/           # should not exist, but just in case
